<div id="editor">
  <div class="form-container">
    <div class="column-1">
      <form id="editor-form">
        <input type="hidden" id="work-id" value="{{work.id}}" />

        <!-- Move the existing works dropdown above the title field -->
        <label for="existing-works">Add to Existing Works</label>
        <select id="existing-works" name="existing-works">
          <option value="">Select an existing work (Optional)</option>
          <option value="new">New Collection</option>
          <!-- Add options dynamically in your JavaScript if needed -->
        </select>

        <!-- New Collection Title field, hidden by default -->
        <label for="collection-title" id="collection-title-label" style="display:none;">Collection Title</label>
        <input
          type="text"
          id="collection-title"
          name="collection-title"
          style="display:none;"
        />

        <label for="title">Title</label>
        <input
          type="text"
          id="title"
          name="title"
          value="{{work.title}}"
          required
        />

        <label for="content">Content</label>
        <textarea
          id="content"
          name="content"
          rows="10"
          required
        >{{work.content}}</textarea>

        <div class="form-buttons">
          <button id="save-button" type="button">Save</button>
          <button id="post-button" type="submit">Post</button>
          <button id="delete-button" type="button">
            <i class="fa-solid fa-trash-can"></i>
            Delete
          </button>
        </div>
      </form>
    </div>
    <div class="column-2">
      <div class="comments-section">
        <h2>Comments</h2>
        {{#if comments.length}}
          {{#each comments}}
            <div class="comment">
              <p>{{this.content}}</p>
              <p class="username">-
                {{this.user.username}}
                on
                {{format_date this.created_at}}</p>
            </div>
          {{/each}}
        {{else}}
          <p>No comments yet.</p>
        {{/if}}
      </div>
    </div>
  </div>
</div>
<script src="/js/editor.js"></script>
