<div id="profile">
  <div class="profile-container">
    <div class="row">
      <!-- Row 1: User Information Section -->
      <div class="section section-1">
        <h2>User Information</h2>
        <!-- Form for editing user information -->
        <form class="user-info-form">
          <!-- Input for the username -->
          <label for="username">Username:</label>
          <input
            type="text"
            id="username"
            name="username"
            value="{{username}}"
          />

          <!-- Input for the first name -->
          <label for="first-name">First Name:</label>
          <input
            type="text"
            id="first-name"
            name="firstName"
            value="{{firstName}}"
          />

          <!-- Input for the last name -->
          <label for="last-name">Last Name:</label>
          <input
            type="text"
            id="last-name"
            name="lastName"
            value="{{lastName}}"
          />

          <!-- Input for the email address -->
          <label for="email">Email Address:</label>
          <input type="email" id="email" name="email" value="{{email}}" />

          <!-- Buttons for saving changes and changing the password -->
          <div class="form-buttons">
            <button type="button" class="save">Save</button>
            <span
              id="userInfoMessage"
              style="color: var(--redvariable); margin-left: 10px;"
            ></span>
            <button
              id="changePasswordBtn"
              type="button"
              class="change-password"
            >Change Password</button>
            <span
              id="changePasswordMessage"
              style="color: var(--redvariable); margin-left: 10px;"
            ></span>
          </div>
        </form>
      </div>

      <!-- Row 1: Profile Image Section -->
      <div class="section section-2">
        <div class="profile-image"></div>
      </div>
    </div>

    <!-- Change Password Modal -->
    <div id="changePasswordModal" class="modal">
      <div class="modal-content">
        <span class="close">&times;</span>
        <h2>Change Password</h2>
        <form id="changePasswordForm">
          <label for="currentPassword">Current Password:</label>
          <input
            type="password"
            id="currentPassword"
            name="currentPassword"
            required
          />

          <!-- Input for the new password -->
          <label for="newPassword">New Password:</label>
          <input type="password" id="newPassword" name="newPassword" required />

          <!-- Input to confirm the new password -->
          <label for="confirmNewPassword">Confirm New Password:</label>
          <input
            type="password"
            id="confirmNewPassword"
            name="confirmNewPassword"
            required
          />

          <!-- Submit button for changing the password -->
          <button type="submit">Change Password</button>
        </form>
      </div>
    </div>

    <div class="row">
      <!-- Row 2: About Me Section -->
      <div class="section section-3">
        <h2>About Me</h2>
        <!-- Textarea for the user bio -->
        <textarea id="bio" name="bio" maxlength="500">{{bio}}</textarea>
        <div class="bio-save-container">
          <!-- Button to save the bio -->
          <button type="button" class="update-bio" id="saveBioBtn">Save Bio</button>
          <span
            id="bioMessage"
            style="color: var(--redvariable); margin-left: 10px;"
          ></span>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Link to external JavaScript files -->
<script src="/js/profile.js"></script>
<script src="/js/changePassword.js"></script>